name: Actions diagnostic

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runner-check:
    runs-on: self-hosted
    steps:
      - name: Runner is available
        run: |
          echo "Self-hosted GitHub Actions runner is available"
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Commit: $GITHUB_SHA"
